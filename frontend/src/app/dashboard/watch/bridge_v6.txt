div className="flex-1 overflow-y-auto p-8 space-y-6 custom-scrollbar bg-[radial-gradient(circle_at_bottom_right,_var(--tw-gradient-stops))] from-accent-magenta/5 via-transparent to-transparent">
             <div className="h-full flex flex-col items-center justify-center text-center opacity-50">
                <Loader2 size={40} className="animate-spin mb-4 text-accent-orange" />
                <p className="text-sm">Finding your location...</p>
             </div>
             <p className="text-center py-20 opacity-30 italic">Loading conversation history...</p>
             <div className="h-full flex flex-col items-center justify-center text-center opacity-30">
                <MessageSquare size={48} className="mb-4" />
                <p className="text-sm">No chat activity here yet.<br/>Start a conversation to say hello!</p>
             </div>
             messages.map((msg, i) => {
               const isMe = msg.user._id === user?.id;
               return (
               
